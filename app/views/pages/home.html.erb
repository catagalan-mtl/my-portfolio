<%# <script>
  const theme = localStorage.getItem("theme") || "dark";
  document.body.classList.add(theme);
</script> %>

<%= render 'hero' %>
<main>
  <%= render 'skills' %>
  <%= render 'projects' %>
  <%= render 'contact' %>
</main>
